import styled from "styled-components";

const EditCarStyles = styled.main`
  display: flex;
  flex-direction: column;

  width: 80vw;
  min-height: 100vh;

  margin: 0 auto;
  padding: 10vh 0;

  background-color: var(--bg);
`;

export default EditCarStyles;
