import styled from "styled-components";

const DriverStyles = styled.main`
  display: flex;
  flex-direction: column;

  width: 80vw;
  min-height: 100vh;

  margin: 0 auto;
  padding: 10vh 0;
`;

export default DriverStyles;
